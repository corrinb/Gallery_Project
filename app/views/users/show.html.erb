<div class="row">
<div class="small-10 columns">
  <h1><%= @user.username %></h1>
</div>
</div>

<div class="row">
<div class="small-10 columns">
  <% @user.images.each do |image| %>
    <%= link_to (image_tag(image.submission, size: "200x200")), image_path(image) %>
  <% end %>
</div>
</div>
