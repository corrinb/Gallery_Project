<% @galleries.each do |gallery| %>
  <%= gallery.theme %>
  <br>
  <% gallery.images.each do |image| %>
    <%= link_to (image.submission), gallery_image_path(gallery, image) %>
  <% end %>
  <br>
  <br>
<% end %>
